/** * Created by wangxiaoyu on 2017/8/16. */import React , {Component} from 'react';class ClickCounter extends Component {    constructor(props){        super(props);        this.state = {            count:0,            title:'click me'        };        this.onClickButton = this.onClickButton.bind(this);    }    onClickButton(){        this.setState({            count:this.state.count+1        })    }    render(){        const counterStyle = {            margin:'20px',            color:'red'        };        return(            <div style={counterStyle}>                <button onClick={this.onClickButton}>{this.state.title}</button>                <div>                    Click Count: {this.state.count}                </div>            </div>        )    }}export  default  ClickCounter;