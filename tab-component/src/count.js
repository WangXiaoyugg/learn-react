/** * Created by wangxiaoyu on 2017/8/15. */import React, { Component } from 'react';class Counter extends Component {    constructor(props){        super(props)        this.handleClick = this.handleClick.bind(this);        this.decrease = this.decrease.bind(this);        this.state={            count:0,        }    }    handleClick(e){        e.preventDefault();        this.setState({            count:this.state.count + 1        })    }    decrease(e){        e.preventDefault();        this.setState({            count: this.state.count - 1        })    }    render(){        return (            <div>                <p>{this.state.count}</p>                <a href="#" onClick={this.handleClick}>增加</a>                <a href="#" onClick={this.decrease}>减少</a>            </div>        )    }}export default Counter;